/** How long the welcome splash stays visible before Home loads. */
export const SPLASH_DURATION_MS = 2500;

/** Undo snackbar visible time before a delete becomes permanent. */
export const UNDO_DURATION_MS = 4000;

/** Used to stack multiple undo snackbars above the bottom edge. */
export const SNACKBAR_HEIGHT = 56;
export const SNACKBAR_GAP = 8;
